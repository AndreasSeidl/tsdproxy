# Dockerfile.dev
#
FROM golang:1.26.1-alpine

WORKDIR /app

# ARG UID=1000
# ARG GID=1000
#
# RUN groupadd -g $GID appgroup && useradd -l -m -u $UID -g appgroup appuser
#

#USER appuser

RUN go install github.com/air-verse/air@latest && go install github.com/a-h/templ/cmd/templ@v0.3.865

COPY go.mod go.sum ./

RUN go mod download

EXPOSE 8080
EXPOSE 7331

# CMD ["make", "server_start"]

CMD ["air", "-c", ".air.toml"]
